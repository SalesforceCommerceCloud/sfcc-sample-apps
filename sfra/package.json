{
  "scripts": {
    "build": "lerna run build --stream",
    "pub": "lerna run pub --stream",
    "test": "jest",
    "integration": "jest -c integration.test.js"
  },
  "dependencies": {
    "completion": "^1.0.1",
    "jest": "^23.6.0"
  },
  "devDependencies": {
    "jest-cli": "^23.6.0",
    "lerna": "^2.11.0"
  }
}
